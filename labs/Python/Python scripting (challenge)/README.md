<h1>Challenge Lab: Python Scripting Exercise</h1>
<h2>Goal off the challenge</h2>
<p>The purpose of the challenge is to create a Python script that identifies prime numbers between 1 and 250. The script must then display those prime numbers on the screen and save them in a text file called <b>results.txt</b>. After writing the script, I must run it to confirm it works correctly and verify that the output was saved in the file. Finally, I must record the absolute path of the script file for future reference.</p>
<h3>STEP 1: SSH into my instance</h3>

<img width="434" height="436" alt="Screenshot 2026-03-09 123835" src="https://github.com/user-attachments/assets/6ab1b398-ac05-46c0-9e92-0d59dcfa8d96" />

<h3>STEP 2: Create the python script</h3>
<p>I Open the Linux terminal and create a new Python file using a <b>nano</b> text editor. I type the command to create a file called <b>prime_numbers.py</b>. This file will contain the Python code needed to calculate prime numbers. Once the file is opened in the editor, I write the Python code that checks numbers from 1 to 250 and determines which numbers are prime. A prime number is a number greater than 1 that can only be divided by 1 and itself... I then save the script and exit the editor.The script is designed to automatically create a file called <b>results.txt</b>. All the prime numbers that were generated will be written into this file. This ensures that the output is saved and can be accessed later without running the script again.</p>

<img width="436" height="552" alt="Screenshot 2026-03-09 124236" src="https://github.com/user-attachments/assets/d43fc63e-9b64-4a23-bdf9-188ae5ad93cf" />

<h3>STEP 3: Run the python script</h3>
<p>I execute the script using Python 3. This is done by running the command <b>python 3 prime_numbers.py</b>. Running the script will cause Python to execute the code, calculate the prime numbers between 1 and 250, and display them on the terminal screen.</p>
<h3>STEP 4: Verify the results</h3>
<p>After the script finishes running, I check whether the <b>results.txt</b> file was created and confirm that it contains the correct list of prime numbers. This can be done by opening the file or displaying its contents in the terminal. This step ensures that the script worked as expected.</p>

<img width="604" height="708" alt="Screenshot 2026-03-09 124502" src="https://github.com/user-attachments/assets/f4c4ee7b-c5f8-4db1-92e7-8cc14ab22da5" />


<p><b>ALL PRIME NUMBERS ARE THERE AND THIS IS THE END OF MY CHALLENGE.</b></p>
